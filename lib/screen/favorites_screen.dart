import 'package:flutter/material.dart';

class FavoritesScr extends StatelessWidget {
  const FavoritesScr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}